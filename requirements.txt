# Core dependencies
lxml>=4.9.0                 # XML parsing for Scopus API
python-dotenv>=0.19.0       # Environment variable management

# Keyword extraction
rake-nltk>=1.0.6            # RAKE keyword extraction algorithm

# Note: CrossRef API support uses only standard library (json, urllib)
# No additional dependencies required for CrossRef functionality

# Optional: Topic modeling (requires external HDP tools)
# numpy>=1.21.0
# scipy>=1.7.0
